# skills folder

Reusable AI skills and workflows.

Each skill lives in its own folder and uses `SKILL.md` as its single canonical
workflow definition. Supporting references, templates and scripts belong inside
that skill folder when they are specific to the skill.
