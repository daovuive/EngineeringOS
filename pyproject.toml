[project]
name = "engineering-os"
version = "1.0.0"
description = "Personal Engineering Knowledge Platform powered by Local AI"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
