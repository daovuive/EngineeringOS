Open `prompts/EngineeringOS_Local_AI_End_to_End_Practice.md`.

Follow the Execution Gate.

Resume from the current `IN PROGRESS` phase; if none exists, start the first `NOT STARTED` phase after the completed phases.

Read only the Execution Gate and that phase. Follow `AGENTS.md`, use the minimum required project context, execute and validate only that phase, then stop and report the result.

Do not read or start later phases unless I explicitly ask.